/* var btn, element;

const obtenerInformacion = (btn)=>{
    return new Promise((resolve, reyect)=>{
    btn = document.querySelectorAll('.btn-desp');
    setTimeout(()=>{resolve(btn)}, Math.random()*600)
    })
}

obtenerInformacion(btn).then(res => console.log(res));

 prop = element.parentNode.parentNode.parentNode.nextElementSibling;
console.log(prop);
element.addEventListener('click', displayTabla(element));

function displayTabla(element){
    if (prop.style.display == 'none'){
        prop.style.display = 'block';
    }
    else{
         prop.style.display = 'none';
    } 
}

for (i = 0; i < resultado.length; i++) {
resultado[i].addEventListener("click", displayTabla) */ 
